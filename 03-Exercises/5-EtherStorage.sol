// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

/** @dev Crea un contrato de billetera simple en Solidity que permita a los usuarios depositar y retirar Ethe.
 * Puedes usar el contrato Storage para inspirarte. Recuerda que el manejo de Ether en un contrato tiene unas consideraciones especiales
 * Puedes Usar este template para completar el ejercicio o reescribirlo a tu manera:
*/

contract Wallet {
    address public owner;
    uint public balance;

    // Constructor: asignar el propietario inicial y establecer el saldo en cero
    constructor() {
        
    }

    // Función para depositar Ether en la billetera
    function deposit() /**MODIFICADOREs */ {
        
    }

    // Función para permitir al propietario retirar Ether
    function withdraw(uint amount) /**MODIFICADOREs */ {
        
    }
}